//Goal of this challenge is to change the array of strings to all lower case
// create a var with a string
// change font to all lower case using .toLowerCase

var words = ["HeLlo", "STrangER", "RuNNing"]

for (var i = 0; i < words.length; i++) {
console.log(words[i].toLowerCase());
}